# Holdim Admin API

## Installation

1. Install Docker on your system

2. Build containers

```
cd docker-compose
docker-compose up
```

3. Run seed

```
docker exec -it holdim_admin_api yarn db:seed
```
